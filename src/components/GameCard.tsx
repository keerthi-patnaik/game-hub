import { Card, CardBody, Heading, Stack } from '@chakra-ui/react';

type GameCardProps = {};

const GameCard = () => {
  return (
    <Card>
      <CardBody>
        <Stack>
          <Heading></Heading>
        </Stack>
      </CardBody>
    </Card>
  );
};

export default GameCard;
